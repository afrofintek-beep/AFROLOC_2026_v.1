node_modules
# Build outputs — anchored to the project root so source folders named "dist"
# (e.g. src/screens/dist/) are NOT ignored.
/dist
/dist-ssr
/dev-dist
*.local
.DS_Store
.vite
# Capacitor native projects (regenerated by `cap add`) — root only.
/android
/ios
.env
.env.*

# TypeScript build cache
*.tsbuildinfo
